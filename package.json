{
	"name": "washazon",
	"version": "1.0.0",
	"description": "A commerce site",
	"devDependencies": {
		"turbo": "^1.12.3"
	},
	"scripts": {
		"build": "turbo type-check build",
		"dev": "turbo dev",
		"dev:database": "docker compose up -d && turbo dev",
		"add:backend": "pnpm add $PACKAGE --filter ./apps/backend",
		"add:frontend": "pnpm add $PACKAGE --filter ./apps/frontend"
	}
}
